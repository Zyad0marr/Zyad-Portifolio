export interface SkillCategory {
  category: string;
  subtitle: string;
  icon: string;
  skills: string[];
}

export interface ServiceItem {
  id: string;
  title: string;
  description: string;
  iconName: string;
  tag: string;
}
